# 2026-05-05

- 在 `/opt/thism` 修改首页 `Dashboard` 的状态筛选：将 `frontend/src/components/dashboard/NodeFilters.tsx` 中的下拉框改为 `All / Online / Offline` 按钮组。
- 同步更新测试：`frontend/src/components/dashboard/dashboard-filtering.test.tsx` 与 `frontend/src/__tests__/dark-contrast-controls.test.tsx`，改为校验按钮组、`aria-pressed` 与深色样式。
- 验证通过：`npm test -- --run src/components/dashboard/dashboard-filtering.test.tsx src/__tests__/dark-contrast-controls.test.tsx`。
- 已执行 `make dev-restart TOKEN=*** PORT=12026` 重建前端并重启服务。
- 浏览器验证：首页已显示按钮组；点击 `ONLINE` 后仅展示在线节点卡片。
- 按主人要求继续统一设置页：将 `frontend/src/components/settings/NodesTable.tsx` 的 `Settings status filter` 从下拉框改为同风格按钮组。
- 同步更新设置页测试：`frontend/src/components/settings/settings-table.test.tsx`，改为校验按钮组、`aria-pressed` 和筛选行为。
- 验证通过：`npm test -- --run src/components/settings/settings-table.test.tsx src/components/settings/batch-upgrade.test.tsx`。
- 已再次执行 `make dev-restart TOKEN=*** PORT=12026`。
- 浏览器验证：设置页 `OFFLINE` 按钮选中时，表格只显示离线节点，按钮状态正确。
- 修复设置页 `Alerts -> Notifications` 的 Telegram 目标交互：`Add target` 不再直接新增，而是先弹确认框；同时每个目标卡片新增 `Remove target`，支持删除并带确认框。
- 相关文件：`frontend/src/components/settings/NotificationsCard.tsx`、`frontend/src/components/settings/notifications-card.test.tsx`、`frontend/src/i18n/messages.en.ts`、`frontend/src/i18n/messages.zh-CN.ts`。
- 验证通过：`npm test -- --run src/components/settings/notifications-card.test.tsx`。
- 已执行 `make dev-restart TOKEN=*** PORT=12026`。
- 浏览器验证：点击 `Add target` 先出现确认弹窗，确认后才新增目标；点击新目标的 `Remove target` 后可删除，删除后目标数量恢复。
- 继续修复 Notifications 页剩余 UX 问题：
  - `Send test notification` 现在会在缺少 token 或目标时禁用，并显示条件提示。
  - token 输入框增加未保存提示，输入新 token 后会显示“有未保存修改”。
  - 三个通知开关的状态文案不再写死中文，改为走 i18n（英文下显示 `Enabled` / `Disabled`）。
- 这次涉及文件：`frontend/src/components/settings/NotificationsCard.tsx`、`frontend/src/components/settings/notifications-card.test.tsx`、`frontend/src/i18n/messages.en.ts`、`frontend/src/i18n/messages.zh-CN.ts`。
- 验证通过：`npm test -- --run src/components/settings/notifications-card.test.tsx`。
- 已执行 `make dev-restart TOKEN=*** PORT=12026`。
- 浏览器验证：英文界面下 token 提示文案出现，通知开关状态显示为 `Enabled`；输入草稿 token 后出现未保存提示。
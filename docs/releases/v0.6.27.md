# {{VERSION}}

## English

Release {{VERSION}}.

### Changes

- Added managed offline GeoIP settings for MaxMind and IP2Location, including provider selection, credential storage, database status, and in-place database updates from the Monitoring settings page.
- Changed the default GeoIP data directory to `/var/lib/thism/geo` while preserving automatic discovery of existing `/opt/1panel/geo` installations.
- Fixed system-pressure and memory-pressure visibility switches so changes save immediately and hidden sections remain hidden on node detail pages.
- Removed internal planning and memory files from the public repository and scrubbed the retired development credential name from public documentation.

### Assets

- Signed linux/amd64 and linux/arm64 agent binaries are attached.
- Signature and version sidecar files are attached for each agent binary.
- Container images are published to GHCR as {{VERSION}}, sha-{{SHORT_SHA}}, and latest.

## 中文

发布 {{VERSION}}。

### 更新内容

- 新增可管理的离线 GeoIP 设置，支持 MaxMind 和 IP2Location 数据源选择、凭据保存、数据库状态查看，以及在监控设置页直接更新数据库。
- GeoIP 数据默认目录调整为 `/var/lib/thism/geo`，同时继续自动识别已有的 `/opt/1panel/geo` 安装。
- 修复系统压力与内存压力显示开关；设置现在会立即保存，关闭后节点详情页会持续隐藏对应区块。
- 从公开仓库移除内部计划和记忆文件，并清理公开文档中已停用的开发凭据名称。

### 发布产物

- 已附带签名的 linux/amd64 和 linux/arm64 agent 二进制文件。
- 每个 agent 二进制文件都附带对应的签名和版本 sidecar 文件。
- 容器镜像已发布到 GHCR，标签包括 {{VERSION}}、sha-{{SHORT_SHA}} 和 latest。
